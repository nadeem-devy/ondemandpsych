export function SectionLabel({ children }: { children: React.ReactNode }) {
  return (
    <span className="text-base font-semibold tracking-[0.2em] uppercase text-[#FDB02F]">
      {children}
    </span>
  );
}
